# Ship loading strategy optimizer
### Build
`cargo build --release`

### Usage
`./ASTAR-calls.sh`

It will create output results on [ASTAR-tests](./ASTAR-tests) folder where test cases are also stored and easily modifiable.